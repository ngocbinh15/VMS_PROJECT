@model List<LANHossting.Models.Kho>
@{
    ViewData["Title"] = "Danh Sách Kho";
}

<div class="container mt-4">
    <h1 class="mb-4">📦 Danh Sách Kho</h1>
    
    <div class="mb-3">
        <a href="@Url.Action("Index", "TestDb")" class="btn btn-secondary">← Quay lại</a>
    </div>

    <div class="card">
        <div class="card-body">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Mã Kho</th>
                        <th>Tên Kho</th>
                        <th>Loại Kho</th>
                        <th>Kho Mẹ</th>
                        <th>Địa Chỉ</th>
                        <th>Người Quản Lý</th>
                        <th>Trạng Thái</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td><strong>@item.MaKho</strong></td>
                                <td>@item.TenKho</td>
                                <td>
                                    <span class="badge @(item.LoaiKho == "KHO_ME" ? "bg-primary" : "bg-info")">
                                        @item.LoaiKho
                                    </span>
                                </td>
                                <td>@(item.KhoMe?.TenKho ?? "-")</td>
                                <td>@item.DiaChi</td>
                                <td>@item.NguoiQuanLy</td>
                                <td>
                                    <span class="badge @(item.TrangThai == "Hoạt động" ? "bg-success" : "bg-secondary")">
                                        @item.TrangThai
                                    </span>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" class="text-center text-muted">Chưa có dữ liệu</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
